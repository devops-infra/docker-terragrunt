schemaVersion: 2.0.0

commandTests:
  - name: Google Cloud SDK
    command: gcloud
    envVars:
      - key: EXPECTED_GCP_VERSION
        value: ${GCP_VERSION}
    args: [--version]
    expectedOutput:
      - ^Google Cloud SDK ${EXPECTED_GCP_VERSION}$
