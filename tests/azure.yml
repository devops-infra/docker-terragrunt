schemaVersion: 2.0.0

commandTests:
  - name: Azure CLI
    command: az
    envVars:
      - key: EXPECTED_AZ_VERSION
        value: ${AZ_VERSION}
    args: [--version]
    expectedOutput:
      - ^azure-cli ${EXPECTED_AZ_VERSION}$
