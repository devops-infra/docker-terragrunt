schemaVersion: 2.0.0

commandTests:
  - name: hcledit
    command: hcledit
    args: [version]
    expectedOutput: [0.*]

  - name: sops
    command: sops
    args: [--version]
    expectedOutput: [sops .*]

  - name: Other tools
    command: which
    args:
      - curl
      - bc
      - docker
      - git
      - go
      - gpg
      - dot
      - hub
      - hcledit
      - jq
      - make
      - task
      - tput
      - ssh
      - openssl
      - python3
      - pip3
      - unzip
      - wget
      - zip
    expectedOutput:
      - /usr/bin/curl
      - /usr/bin/bc
      - /usr/bin/docker
      - /usr/bin/git
      - /usr/bin/go
      - /usr/bin/gpg
      - /usr/bin/dot
      - /usr/bin/hub
      - /usr/bin/hcledit
      - /usr/bin/jq
      - /usr/bin/make
      - /usr/bin/task
      - /usr/bin/tput
      - /usr/bin/ssh
      - /usr/bin/openssl
      - /usr/bin/python3
      - /usr/bin/pip3
      - /usr/bin/unzip
      - /usr/bin/wget
      - /usr/bin/zip

  - name: Python modules
    command: pip
    args: [list]
    expectedOutput:
      - cloudflare
      - PyGithub
      - python-hcl2
      - requests
      - slack_sdk
      - anyio
      - zipp
